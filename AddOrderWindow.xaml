<Window x:Class="DEMO1.AddOrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавление заказа" 
        Height="450" 
        Width="500" 
        WindowStartupLocation="CenterOwner">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Добавление нового заказа" 
                   Grid.Row="0" 
                   Grid.ColumnSpan="2"
                   FontSize="18" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>

        <TextBlock Text="Номер заказа:" Grid.Row="1" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
        <TextBox x:Name="OrderNumberBox" Grid.Row="1" Grid.Column="1" Margin="5" Height="30"/>

        <TextBlock Text="Дата заказа:" Grid.Row="2" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
        <DatePicker x:Name="OrderDatePicker" Grid.Row="2" Grid.Column="1" Margin="5" Height="30"/>

        <TextBlock Text="Дата доставки:" Grid.Row="3" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
        <DatePicker x:Name="DeliveryDatePicker" Grid.Row="3" Grid.Column="1" Margin="5" Height="30"/>

        <TextBlock Text="Пункт выдачи:" Grid.Row="4" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
        <ComboBox x:Name="PickupPointBox" Grid.Row="4" Grid.Column="1" Margin="5" Height="30" IsEditable="True"/>

        <TextBlock Text="ФИО клиента:" Grid.Row="5" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
        <ComboBox x:Name="ClientNameBox" Grid.Row="5" Grid.Column="1" Margin="5" Height="30" IsEditable="True"/>

        <TextBlock Text="Статус:" Grid.Row="6" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
        <ComboBox x:Name="StatusBox" Grid.Row="6" Grid.Column="1" Margin="5" Height="30">
            <ComboBoxItem Content="Новый"/>
            <ComboBoxItem Content="Завершен"/>
            <ComboBoxItem Content="Отменен"/>
            <ComboBoxItem Content="В обработке"/>
        </ComboBox>

        <StackPanel Grid.Row="7" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20">
            <Button Content="Сохранить" Width="100" Height="35" Margin="10" Click="SaveButton_Click" Background="#DEB887"/>
            <Button Content="Отмена" Width="100" Height="35" Margin="10" Click="CancelButton_Click" Background="#DEB887"/>
        </StackPanel>
    </Grid>
</Window>