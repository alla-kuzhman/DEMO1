<Window x:Class="DEMO1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DEMO1"
        mc:Ignorable="d"
        Title="Магазин Обуви"
        Height="350" 
        Width="500"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Добро пожаловать в магазин 'Обувь'"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,20"
                   Grid.Row="0"
                   HorizontalAlignment="Center"/>

        <TextBlock Text="Логин:" Grid.Row="1" Margin="0,10,0,0"/>
        <TextBox x:Name="LoginBox" Margin="0,5,0,0" Grid.Row="2" Height="30"/>

        <TextBlock Text="Пароль:" Grid.Row="3" Margin="0,10,0,0"/>
        <PasswordBox x:Name="PasswordBox" Margin="0,5,0,0" Grid.Row="4" Height="30"/>

        <StackPanel Orientation="Horizontal" 
                    HorizontalAlignment="Center"
                    Margin="0,20,0,0"
                    Grid.Row="5">
            <Button x:Name="LoginButton"
                    Content="Войти"
                    Width="100"
                    Height="35"
                    Margin="10"
                    Click="LoginButton_Click"/>
            <Button x:Name="GuestButton"
                    Content="Продолжить как гость"
                    Width="150"
                    Height="35"
                    Margin="10"
                    Click="GuestButton_Click"/>
        </StackPanel>

        <TextBlock x:Name="ErrorText"
                   Foreground="Red"
                   Grid.Row="6"
                   HorizontalAlignment="Center"
                   Margin="0,10"/>
    </Grid>
</Window>